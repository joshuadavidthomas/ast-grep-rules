id: no-deny-warnings
language: Rust
severity: warning
message: "Do not use #![deny(warnings)]; configure explicit lint levels instead."
note: "Denying all warnings can break builds when the compiler or dependencies add new warnings."
rule:
  kind: inner_attribute_item
  regex: "^#!\\[deny\\(warnings\\)\\]$"
