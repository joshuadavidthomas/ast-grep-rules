id: no-hand-rolled-object-type-guard
snapshots:
  ? |
    const isRecord = (value: unknown): value is Record<string, unknown> =>
      value !== null && typeof value === "object" && !Array.isArray(value);
  : labels:
    - source: |-
        const isRecord = (value: unknown): value is Record<string, unknown> =>
          value !== null && typeof value === "object" && !Array.isArray(value);
      style: primary
      start: 0
      end: 142
    - source: typeof value === "object"
      style: secondary
      start: 91
      end: 116
  ? |
    const isRecord = (value: unknown): value is Record<string, unknown> => {
      return value !== null && typeof value === 'object' && !Array.isArray(value);
    };
  : labels:
    - source: |-
        const isRecord = (value: unknown): value is Record<string, unknown> => {
          return value !== null && typeof value === 'object' && !Array.isArray(value);
        };
      style: primary
      start: 0
      end: 154
    - source: typeof value === 'object'
      style: secondary
      start: 100
      end: 125
  ? |
    const isRecordLike = (value: unknown): value is Record<string, string> => {
      return value !== null && typeof value === 'object' && !Array.isArray(value);
    };
  : labels:
    - source: |-
        const isRecordLike = (value: unknown): value is Record<string, string> => {
          return value !== null && typeof value === 'object' && !Array.isArray(value);
        };
      style: primary
      start: 0
      end: 157
    - source: typeof value === 'object'
      style: secondary
      start: 103
      end: 128
  ? |
    function isObject(value: unknown): value is object {
      return value !== null && typeof value === 'object';
    }
  : labels:
    - source: |-
        function isObject(value: unknown): value is object {
          return value !== null && typeof value === 'object';
        }
      style: primary
      start: 0
      end: 108
    - source: typeof value === 'object'
      style: secondary
      start: 80
      end: 105
  ? |
    function isRecord(value: unknown): value is Record<string, unknown> {
      return value !== null && typeof value === 'object' && !Array.isArray(value);
    }
  : labels:
    - source: |-
        function isRecord(value: unknown): value is Record<string, unknown> {
          return value !== null && typeof value === 'object' && !Array.isArray(value);
        }
      style: primary
      start: 0
      end: 150
    - source: typeof value === 'object'
      style: secondary
      start: 97
      end: 122
  ? |
    function isUnknownRecord(input: unknown): input is Record<string, unknown> {
      return input !== null && typeof input === "object" && !Array.isArray(input);
    }
  : labels:
    - source: |-
        function isUnknownRecord(input: unknown): input is Record<string, unknown> {
          return input !== null && typeof input === "object" && !Array.isArray(input);
        }
      style: primary
      start: 0
      end: 157
    - source: typeof input === "object"
      style: secondary
      start: 104
      end: 129
  ? |
    function isUser(value: unknown): value is User {
      return (
        value !== null &&
        typeof value === 'object' &&
        'id' in value &&
        typeof value.id === 'string'
      );
    }
  : labels:
    - source: |-
        function isUser(value: unknown): value is User {
          return (
            value !== null &&
            typeof value === 'object' &&
            'id' in value &&
            typeof value.id === 'string'
          );
        }
      style: primary
      start: 0
      end: 175
    - source: typeof value === 'object'
      style: secondary
      start: 86
      end: 111
